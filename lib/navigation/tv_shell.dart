import 'package:flutter/material.dart';

class TvShell extends StatelessWidget {
  const TvShell({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Viikshana TV Shell')),
    );
  }
}